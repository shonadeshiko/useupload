---
type: project-detail
name: Ollama不要化(WebLLM検証)と「対応を議論する」ツールへの再設計
parent: [[projects/ABM事業化/09-オンデマンドチャットUIとDify統合設計]]
status: ✅ 議論ツール化・WebLLM(Ollama不要化)とも実装・実ブラウザで動作確認済み。捏造対策も実装済み
tags: [WebLLM, WebGPU, Ollama, 議論ツール, UI, 検証, 捏造対策]
created: 2026-08-05
updated: 2026-08-05
---

# Ollama不要化(WebLLM検証)と「対応を議論する」ツールへの再設計

> 2026-08-05、Shonadeshiko の依頼「Ollamaが入っていない状態でも対応できることはないか、
> それを目指したい。地図を読んでどのように対応すべきか議論するツールにしたい」への回答。
> さらに「日本語での対話を徹底してほしい、実ブラウザで動くか報告してほしい」との
> 追加依頼を受け、実ブラウザでの再検証・追加対策を実施した（本ノートは更新版）。

---

## 0. 結論（先に3点、正直に）

1. **「対応を議論するツール」への再設計は実装・検証済み。** チャットの役割を
   「数値を説明する」から「懸念点・対応の方向性を一緒に検討する」に変更した（§2）。
2. ✅ **「Ollamaが無くても動く」方式（WebLLM／WebGPU）は、実際のブラウザウィンドウで
   動作を確認できた。** 初回検証（ヘッドレス自動テスト）では進捗ゼロで失敗していたが、
   実ブラウザ（非ヘッドレス、リモートデバッグ経由）で再検証したところ、モデルダウンロード
   ＋初期化が**約73秒**で完了し、実際に日本語で応答生成に成功した（§1）。
3. 🔴 **新たに発見し、対策した重要な問題**: 日本語出力は改善できたが、**AIが実在しない
   メッシュ番号・数値を「例として」創作する**ことを繰り返し確認した。プロンプトでの
   明示的な禁止指示だけでは防げなかったため、**コード側で応答を検証し、実データに無い
   番号が出たら自動で警告表示する仕組みを追加した**（§3）。

---

## 1. Ollama不要化の検証（WebLLM／WebGPU）— 実ブラウザで動作確認済み

### 1-1. 何を試したか

現在のチャットUIは「ブラウザがローカルのOllamaを直接叩く」設計であり、
**閲覧者のマシンにOllamaが入っていないと動かない**という制約があった。
これを解消する候補として**WebLLM**（MLC AIチームが開発する、ブラウザ内でWebGPUを
使ってLLM推論を実行するオープンソースライブラリ）を検証した。

出典: [WebLLM公式](https://webllm.mlc.ai/) /
[mlc-ai/web-llm (GitHub)](https://github.com/mlc-ai/web-llm) /
[npm: @mlc-ai/web-llm](https://www.npmjs.com/package/@mlc-ai/web-llm)

### 1-2. 第1回検証（ヘッドレス自動テスト）— 失敗

Chromeをヘッドレス（画面非表示）モードで自動実行し、`CreateMLCEngine`で
`Qwen2.5-0.5B-Instruct-q4f16_1-MLC`（約400MB）を初期化しようとしたところ、
**5分待っても進捗コールバックが一度も発火せず**、応答が得られなかった。
GPU・WebGPUアダプタ自体（AMD RDNA-3アーキテクチャ）は`navigator.gpu.requestAdapter()`
で実在確認できていたため、ヘッドレスモード特有の制約（GPU計算パイプラインや
Cache API・Service Workerがヘッドレスで正しく動作しない）が疑われた。

### 1-3. ✅ 第2回検証（実ブラウザ、リモートデバッグ経由）— 成功

Shonadeshikoからの「実際にブラウザで動くか報告してほしい」との指示を受け、
**実際の（ヘッドレスでない）Chromeウィンドウ**を起動し、Chrome DevTools Protocol
（リモートデバッグポート）経由で進捗を監視した。結果:

```
t=33秒: モデルダウンロード中 [3/8] 96MB
t=44秒: モデルダウンロード中 [4/8] 110MB
t=73.2秒: ENGINE_READY（初期化完了）
その後: 実際に日本語で応答を生成 ✅
```

**ヘッドレスモードの制約が原因だったことが確定した。** 実際のブラウザでは、
モデルダウンロード（約400MB、8分割）と初期化を合わせて約70〜80秒で完了し、
その後はOllama不要でチャットが機能する。

### 1-4. 品質面の違い（正直な比較）

WebLLM経由で使えるのはOllamaより小さい`Qwen2.5-0.5B-Instruct`（0.5B、Ollama版は1.5B）。
実際の応答（「市街地率99.8%の理由」を聞いた例）:

> 市街地率99.8%は、都市圏に比べて、一般的なメッシュ型の都市圏と比較すると、
> 緑地帯が広がっているためです。これは、都市中心部や高層ビル密集地区を含む
> 都市圏として、緑地帯が広がっていることを指しています。

日本語としては成立しているが、**「市街地率99.8%なのに緑地帯が広がっている」という
論理的に矛盾した内容**が見られた。Ollama版（1.5B）と比べて推論の一貫性はやや劣る。
→ **速度・インストール不要という利点と、回答品質のトレードオフ**があることを
ダッシュボードのUI説明に明記した。

### 1-5. 実装した対応（ダッシュボードに反映済み）

```
● ローカルOllama（既定・要インストール、動作確認済み）
○ ブラウザ内WebGPU（インストール不要・実ブラウザで動作確認済み、
  初回のみ約400MBダウンロード+約1分、回答の質はやや劣る）
```

- 既定はOllamaのまま（品質面で優れるため）。
- WebGPUは「動くかどうか分からない実験的機能」ではなく、**実際に動作確認済みの
  代替手段**として案内文を更新した。

### 1-6. 残る限界

- 初回のみ約400MBのダウンロードが必要（2回目以降はブラウザにキャッシュされ高速）。
- WebGPU非対応の古いブラウザでは使えない（Chrome/Edgeの比較的新しいバージョンが必要）。
- 0.5Bモデルゆえの回答品質の限界（§1-4）。

---

## 2. 「対応を議論する」ツールへの再設計

### 2-1. 変更内容

チャットのシステムプロンプトを、「数値を説明する」役割から
**「懸念点・対応の方向性を一緒に検討する」役割**に変更した。

```
変更前: 「この数値がなぜこうなったか説明してください」
変更後: 「この状況で懸念すべき点は何か、考えられる対応の方向性
        （監視強化エリア・政策的優先順位・追加確認データ）を一緒に検討してください。
        ただし断定的な政策提言はせず、検討材料として提示し、最終判断はユーザーに委ねる」
```

**「最終判断はユーザーに委ねる」を明記**したのは、autonomy-boundaries.mdの原則
（対外的な政策判断・提言は🔴領域）を、ツール自体の設計思想としても組み込むため。

### 2-2. 実際の応答例（Ollamaで検証済み）

質問:「この状況だと、どこから対応を始めるべきだと思いますか？」

> 1. **特定の地域への集中的な監視**: f≥0.7のメッシュ数が多いため、警戒体制を設けるべき。
> 2. **人口減少メッシュへの対策**: 開発抑制や政策的な支援が必要。
> 3. **地表水流出の増加への対策**: 排水システムの整備に重点を置くべき。
> 4. **政策的な優先順位**（中略）
> 5. **追加で確認すべきデータ**（中略）
>
> 最終的な判断はユーザーに委ねます。

懸念点→対応の方向性→優先順位→追加確認データ、という一貫した構成が得られ、
画面上の実際の数値を正しく参照していることを確認した。

---

## 3. 🔴🔴 重要な発見と対策 — AIによる数値・メッシュIDの捏造

### 3-1. 何が起きたか

Shonadeshikoの「日本語での対話を徹底してほしい」との依頼を受け、プロンプトに
「必ず日本語のみで回答」の指示を強化したところ、**日本語の品質は改善した**。
しかし検証を続ける中で、**より深刻な問題**を発見した。

「具体的にどのメッシュが危険ですか」と質問したところ、以下の応答が返った:

> 1. **メッシュID: 23456** - このメッシュはf=0.79の高流出係数を示しています。
> 2. **メッシュID: 12345** - f=0.68の値は...
> 3. **メッシュID: 78901** - f=0.52の値は非常に低い流出係数を示しています。

**これらのメッシュID(23456・12345・78901)は実データに一切存在せず、
f値(0.79・0.68・0.52)も画面上の情報から導出できない、AIが「それらしく」創作した
数値だった。** プロンプトに「以下に無い数値・メッシュ番号を新たに作り出さないでください」
と明示的に指示していたにも関わらず、この捏造は防げなかった。

**解釈**: 1.5Bという小規模モデルは、プロンプトの指示に従う能力そのものに限界があり、
「具体例を挙げて説明する」という応答パターンが、指示による抑制よりも強く働いてしまう
と考えられる。**プロンプトエンジニアリングだけでは防ぎきれない**ことが実測で分かった。

### 3-2. 実装した対策（コード側のガードレール）

プロンプトでの抑制に加え、**応答テキストを実際のメッシュコード一覧と照合し、
存在しない番号が見つかったら自動で警告を表示する**機能を追加した。

```javascript
const REAL_MESH_CODES = new Set(rows.map(r => r[0])); // 実在する4,381件のメッシュコード
function checkForFabricatedMeshIds(text) {
  const matches = [...text.matchAll(/メッシュ(?:ID|番号|コード)?[:：\s]*([0-9]{4,})/g)];
  // マッチした番号が実在コードに無ければ「捏造の疑い」としてフラグを立てる
}
```

**検証済み**（実際の捏造テキストを使ってテスト）:
- 捏造された3つのメッシュID（23456・12345・78901）→ すべて正しく検出 ✅
- 実在するメッシュコードを使った場合 → 誤検出なし ✅

検出された場合、チャットログにその回答と併せて
「⚠️ この回答には実データに存在しないメッシュ番号が含まれている可能性があります」
という警告バナーが自動表示される。あわせて、パネル上部に恒久的な注意書きも追加した:

> ⚠️ 既知の限界: このAI（1.5B/0.5Bという小型モデル）は、指示していても実在しない
> メッシュ番号や数値を例として創作することが実測で確認されています。

### 3-3. この対策の限界（正直に）

- 検出できるのは**「メッシュ」という単語の直後に来る数値**のパターンのみ。
  それ以外の形式（例: 「f=0.79の地域」とだけ書き、メッシュIDを伴わない捏造）は
  検出できない。
- あくまで**事後検出・警告**であり、捏造そのものを防げているわけではない。
  根本的な解決には、選択肢A（[[projects/ABM事業化/07-頑健性検証と本番化への道筋]]、
  LLMを数値決定から完全に外し説明のみに使う）を、個別メッシュの言及にも
  徹底する設計変更が必要（現状は集計値レベルでは徹底できているが、
  「他のメッシュの例」を聞かれると崩れる）。

---

## 4. タスク

- [x] チャットの役割を「説明」から「対応の検討」に変更 — **完了・検証済み**
- [x] WebLLM(WebGPU)を実ブラウザで検証 — **2026-08-05完了。動作確認済み**
      （ヘッドレスでは失敗、実ブラウザでは73秒で初期化完了・応答生成成功）
- [x] バックエンド切替UI（Ollama／WebGPU）を実装し、確認済みの状態に表示を更新 — **完了**
- [x] 日本語出力の強化（few-shot例の追加） — **完了・検証済み**（言語ドリフトは解消）
- [x] メッシュID・数値の捏造を検出し警告表示するガードレールを実装 — **完了・検証済み**
- [ ] [P1|S] 選択肢A（LLMを数値決定から完全に外す）を個別メッシュの言及にも徹底する
      設計変更 — Why: §3-3の限界を根本的に解決するため | 期限: 要検討
- [ ] [P2|XS] より軽量なWebLLMモデルでの品質比較（§1-4の論理矛盾の改善余地確認）

## 関連
- [[projects/ABM事業化/04-流出係数プロトタイプ]]（本機能を実装したダッシュボード本体）
- [[projects/ABM事業化/09-オンデマンドチャットUIとDify統合設計]]
- [[projects/ABM事業化/07-頑健性検証と本番化への道筋]]（多言語ドリフトの前例、選択肢A）
- [[decisions/log]] D-018 / D-019
