---
type: project-detail
name: 地図データ直結型エージェントUI — クリックしたメッシュの実データでOllamaに聞く
parent: [[projects/ABM事業化/07-頑健性検証と本番化への道筋]]
status: 実装済み・ダッシュボードに統合済み
tags: [LLMエージェント, UI, Ollama, 案B, 地図連動]
created: 2026-08-05
updated: 2026-08-05
---

# 地図データ直結型エージェントUI

> 2026-08-05、Shonadeshiko の依頼「UIについてもエージェントを実装してほしい。
> ベースにするのは中身のデータではなく地図上で写っているデータをベースとして
> 対応シミュレーションなどができるようにエージェントベースでやってみたい」への回答。

---

## 0. 何を変えたか（結論）

**これまで**（[[projects/ABM事業化/06-案B設計と限界]] 〜 07）は、
市区町村×地域区分（A/B の2グループ）という**抽象化された代表値**
（市街地化率35.0%・耕地残存率60.0%といった固定のデモ用数値）をエージェントに渡していた。

**今回の変更**: エージェントの入力を、**地図上に実際に表示されている、
クリックしたメッシュそのものの実データ**に変更した。ユーザーが
[[projects/ABM事業化/04-流出係数プロトタイプ]] の地図上の任意のメッシュをクリックすると、
**そのメッシュの現在の計算結果（流出係数・人口指数・土地利用構成）**がそのままエージェントへの
入力になり、ローカルのOllamaがその場で説明文を生成する。

```
【これまで】                          【今回】
市区町村×地域区分(2種類)               地図上でクリックした個別メッシュ(4,381件から任意)
  ↓ 固定の代表値                        ↓ そのメッシュの実際の計算結果
  urban_pct=35.0(仮の代表値)            urban_pct=実際の値(層Bが計算した値そのまま)
  ↓                                    ↓
LLMエージェント                        LLMエージェント
  ↓                                    ↓
一般化された理由文                     そのメッシュ固有の理由文
```

---

## 1. 設計原則

### 1-1. エージェントは「数値を決める」のではなく「表示されている数値を説明する」

[[decisions/log]] D-016（選択肢A）の方針をそのまま踏襲する。
層B（案A・逐次更新モデル）が既に計算した、**そのメッシュの実際の
流出係数・人口指数・土地利用構成**をプロンプトにそのまま埋め込み、
「この数値を新たに作り出さず、そのまま解釈して説明してください」と明示的に指示する。

理由: [[projects/ABM事業化/07-頑健性検証と本番化への道筋]] §1 で確認した通り、
1.5Bモデルに新しい数値を生成させると、穏やかな入力の違いに鈍感という限界がある。
**数値の決定は感度の良い層Bに任せ、LLMは得意な自然文での説明に専念させる**ことで、
この限界を回避しつつ「なぜこの結果になったか」という説明可能性を提供する。

### 1-2. 「地図に表示されている値」＝「今まさに選択中のモード・年・シナリオでの計算結果」

エージェントに渡す数値は、**固定のスナップショットではなく、ユーザーが今まさに
操作しているモード（断面／累積）・年・人口シナリオ・感度パラメータの組み合わせで
計算された、そのメッシュの現在の値**。スライダーを動かして計算結果が変わった場合、
エージェントパネルの数値表示は追従して更新される（ただしLLMの再呼び出しは
自動では行わない。詳細は§3）。

### 1-3. なぜメッシュ単位で個別に呼べるのか（従来の「代表値」方式との違い）

06〜07では「4,381メッシュ全部にLLMを当てると時間がかかりすぎる」ため
市区町村×地域区分の代表値方式を取っていた。今回はこの制約を**オンデマンド化**で
解決する: **あらかじめ全メッシュを計算しておくのではなく、ユーザーがクリックした
その1メッシュだけをその場で呼ぶ**。4,381通りの組み合わせを事前計算する必要がなく、
ユーザーの興味のあるメッシュだけをピンポイントで説明できる。

---

## 2. アーキテクチャ

### 2-1. ブラウザから直接ローカルOllamaを呼ぶ（サーバー不要）

```
ブラウザ(index.html、file://で開いた静的HTML)
    │ fetch('http://localhost:11434/api/generate')
    ▼
ローカルOllama（同じマシン上で稼働中）
    │
    ▼
qwen2.5:1.5b が応答（reasoning文のみ、temperature=0）
```

**確認済み**: Ollamaは `OLLAMA_ORIGINS=*` で起動しており、ブラウザの `fetch()` が
`file://` オリジンから直接叩いてもCORSエラーにならないことを確認済み
（2026-08-05、`curl -X OPTIONS` でpreflightレスポンスの
`Access-Control-Allow-Origin: *` を確認）。
→ **中間サーバー（Flask/FastAPI等）を書く必要がない。** ダッシュボードのHTMLファイルは
引き続き「1ファイル完結の静的サイト」のままで、Ollamaというローカルサービスとだけ通信する。

### 2-2. UIフロー

1. ユーザーが地図上のメッシュをクリック → そのメッシュが選択され、
   実際の値（f・人口指数・土地利用構成）がパネルに表示される。
2. 「このメッシュのエージェントに聞く」ボタンを押す → ローカルOllamaへfetch。
3. 5〜10秒程度で理由文が返り、パネルに表示される。
4. 同じメッシュ・同じシナリオ設定を再度開いた場合は、**ブラウザ内キャッシュ
   （JSの`Map`）から即座に表示**（再度LLMを呼ばない）。
5. モード・年・シナリオ・感度を変えると、パネルの数値表示は追従するが、
   **LLMは自動では再呼び出しされない**（ボタンを再度押す必要がある）。
   → コスト（時間）を無駄にしない設計。

### 2-3. プロンプト（実物）

```
あなたはこの地域(多摩川流域)の土地利用変化を説明する専門家です。
以下は、ある500m四方のメッシュについて、既に別の計算モデルが導き出した結果です。
あなたの役割は、この数値を新たに作り出すことではなく、そのまま解釈して説明することです。

【このメッシュの現在の状態（地図上に実際に表示されている値）】
- 年: {year}年 / 人口シナリオ: {scenarioLabel} / 計算モード: {modeLabel}
- 市街地: {urban}% / 耕地: {crop}% / 森林: {forest}% / 草地(粗放化): {grass}%
- 流出係数: {f}（2022年の実測値との差: {delta}）
- 人口指数: {popIdx}（2020年を1.0とした指数。この間の変化率 {growth}%）

【説明してほしいこと】
このメッシュがなぜこのような状態になったと考えられるか、上記の数値だけを根拠として
2-3文の日本語で説明してください。数値を新たに作り出したり、上記に無い推測の数値を
挙げたりしないでください。
【重要】必ず日本語で書いてください。中国語や英語は使わないでください。

【出力形式(JSON)】
{"reasoning": "<説明文>"}
```

`temperature=0`（[[projects/ABM事業化/07-頑健性検証と本番化への道筋]] §1の頑健性チェックの
結論を反映、決定論的にするため）。

---

## 3. 実装済みの挙動（設計判断の記録）

| 状況 | 挙動 |
|---|---|
| メッシュをクリック | パネルに実データを即時表示。ボタンが有効化される |
| 「エージェントに聞く」を押す | ローカルOllamaへfetch。応答まで5〜10秒、ボタンは無効化されローディング表示 |
| 同じメッシュ・同じシナリオで再度押す | キャッシュから即座に表示（LLM再呼び出しなし） |
| スライダーでシナリオを変更 | パネルの数値表示は更新される。**理由文は自動更新されない**（古いまま）。
再度ボタンを押すと新しい設定での説明が得られる |
| Ollamaが起動していない・応答なし | エラーメッセージを表示（「Ollamaが起動していることを確認してください」）。
沈黙して0を返すような誤魔化しはしない |

---

## 4. 🔴 これで解決していないこと・限界

- **4,381メッシュ全部を事前に説明できるわけではない。** あくまで**ユーザーがクリックした
  メッシュだけ**をその場で説明する設計。全メッシュの一覧的な説明が欲しい場合は、
  06〜07の「市区町村×地域区分」の代表値バッチ方式（事前計算）の方が向く。
  **用途によって使い分ける**: 個別の疑問（「なぜこのメッシュは？」）には本UI、
  全体の物語（「この流域全体でどんな力学が働いているか」）にはバッチ方式。
- **このアーキテクチャは「ローカルで動かす」ことが前提。** ブラウザが直接
  `localhost:11434` を叩く設計のため、**Ollamaが同じマシンで動いていないと機能しない**。
  もしこのHTMLをWebサーバーにアップロードして他人に公開した場合、
  **見る人自身のマシンにOllamaが入っていないとエージェント機能は動かない**
  （地図・数値ダッシュボード自体は引き続き動く。エージェント機能だけが使えなくなる）。
  → この点は「個人のローカル分析ツール」としては強みだが、「不特定多数向けのWeb公開」
  としては制約になる。[[projects/ABM事業化/07-頑健性検証と本番化への道筋]] §3 の
  「道1: 事前計算」と「道2: ライブ方式」の区分でいうと、**本UIは道2の変種
  （サーバーは要らないが、閲覧者自身のOllamaが要る）**という新しい第3の道にあたる。
- 温度0のため決定論的だが、[[projects/ABM事業化/07-頑健性検証と本番化への道筋]] §1-2で
  確認した通り、**穏やかな差には鈍感な可能性がある**。ただし本UIはLLMに数値を決めさせず
  説明のみさせているため、この限界の影響は06〜07の設計より小さいと考えられる
  （検証はしていない）。

---

## 5. タスク

- [x] 地図クリックでメッシュを選択する機能 — **2026-08-05実装完了**
- [x] 選択メッシュの実データをローカルOllamaに渡し説明文を取得する機能 — **実装完了**
- [x] キャッシュ機構（同一条件での再呼び出し防止） — **実装完了**
- [x] エラーハンドリング（Ollama未起動時のメッセージ） — **実装完了**
- [x] Chrome headlessで実際にクリック→メッシュ選択までを検証 — **2026-08-05完了**。
      実際にメッシュ533943233が選択され、f=0.899・市街地99.8%という実データが
      正しくパネルに表示されることを画面キャプチャで確認済み。
- [x] Ollama呼び出しロジック（プロンプト構築・fetch・JSONパース）をNode.jsの
      `fetch()`で直接検証 — **2026-08-05完了**。ブラウザと同一のコードパスで、
      実際に日本語の一貫した説明文（市街地99.8%という状況を都市化の進行として
      正しく解釈）を取得できることを確認済み。
      ※Chrome headlessでの完全な往復（クリック→ボタン押下→10秒待機→表示）は
      ヘッドレスブラウザの仮想時間とネットワークI/Oの相性が悪く自動化検証が
      不安定だったため、上記2つを独立に検証することで代替した。
- [ ] [P2|S] 「道1（バッチ）」と「道2変種（本UI）」を同じダッシュボード内で
      両方使えるようにする（バッチ結果があればそれを初期表示し、無ければ本UIで
      オンデマンド取得する、というフォールバック設計）— Why: 閲覧者にOllamaが
      無い場合でも最低限の説明を見せられる | 期限: 08/10

## 関連
- [[projects/ABM事業化/04-流出係数プロトタイプ]]（本機能を実装したダッシュボード本体）
- [[projects/ABM事業化/06-案B設計と限界]] / [[projects/ABM事業化/07-頑健性検証と本番化への道筋]]
- [[decisions/log]] D-016
