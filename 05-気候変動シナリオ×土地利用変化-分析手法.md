---
type: project-detail
name: 気候変動(SSP)シナリオによる土地利用変化の分析手法
parent: [[projects/ABM事業化/02-実装設計]]
status: 断面モード・累積モード(案A)ともに実装・検証済み。案BはprojID [[projects/ABM事業化/06-案B設計と限界]] 参照
tags: [SSP, 気候変動, 土地利用変化, 分析手法, 多摩川流域]
created: 2026-08-05
updated: 2026-08-05
---

> 🆕 **2026-08-05 追記**: 本ノート§5で提案した案A（逐次更新・不可逆化）は**実装・検証済み**。
> [[projects/ABM事業化/04-流出係数プロトタイプ]] に「断面モード」「累積モード」の切替として実装。
> 案B（LLMエージェント層）は [[projects/ABM事業化/06-案B設計と限界]] を参照
> （アーキテクチャは実装済み・実際のLLM呼び出しは未実行）。

# 気候変動(SSP)シナリオによる土地利用変化の分析手法

> Shonadeshiko からの質問「2022年以降にSSPのシナリオをもとに土地利用の変化を見ることができるか」
> への回答を兼ねる。**結論: できるが、部分的。** 何ができて、何ができていないかを先に明示する。

---

## 0. 結論（先に3点）

1. **「その年・そのシナリオでの土地利用の姿」は見られる。** 対象年（2020〜2050、5年刻み）と
   SSPシナリオ（基準/SSP1-2.6/SSP5-8.5）を選ぶと、その組み合わせにおける土地利用構成
   （市街地・耕地・草地・森林・水域の比率）と流出係数がメッシュ単位で計算される。
   実際に2020→2050まで計算すると、都市化率・森林率は**滑らかに単調推移**する（§2 参照）。
2. 🔴 **ただし「経路」は見ていない。** 現在の手法は各年を**独立に**、常に**2022年の土地被覆を
   出発点として**計算し直す**比較静学（comparative statics）**である。年ごとに前年の状態を
   引き継いで積み上げる**時系列シミュレーション（path-dependent simulation）ではない**。
   → 詳細と、これが何を意味するかは §3 で説明する。
3. **気候変動シナリオ（降雨）と土地利用変化シナリオ（SSP人口）は、現在は別々の軸として計算しており、
   相互作用（例: 頻発する洪水が移住を促す、土地利用変化が地域の気候リスク認知を変える）は
   組み込んでいない。** 本ノートの主題は後者（SSP→土地利用）の手法であり、前者（降雨→流出）は
   [[projects/ABM事業化/02-実装設計]] §1 を参照。

---

## 1. 分析の全体構造

```
気候(降雨)                          社会経済(SSP人口)
   │                                      │
   ▼                                      ▼
降雨量変化倍率 r                    人口指数 P(mesh, year, scenario)
（国交省提言、公表値）              （国土数値情報 実測 + 日本版SSP近似）
   │                                      │
   │                                      ▼
   │                          開発圧 dev% / 管理放棄圧 abn%
   │                          （人口指数の増減 × 感度パラメータ）
   │                                      │
   │                                      ▼
   │                          土地利用構成の変化
   │                          （耕地・森林 → 市街地 / 草地）
   │                          ※各年、常に2022年時点の構成から計算
   │                                      │
   │                                      ▼
   │                          流出係数 f（面積加重平均、告示521号）
   │                                      │
   └──────────────────┬──────────────────┘
                       ▼
              流出強度指数 = f × r
```

**2本の軸が完全に独立**であることに注意。降雨強度倍率 r はスライダーで手動設定する探索値であり、
SSPシナリオの選択とは連動していない（「SSP5-8.5を選んだら自動的に r が上がる」という
連動は実装していない。手動で両方を動かす設計）。連動させる設計は §5 の拡張案で扱う。

---

## 2. 現在できること — 年次推移の実測（2026-08-05、Node.jsで計算・検証済み）

対象年を2020年から2050年まで動かし、SSPシナリオ（基準＝社人研中位／SSP1-2.6／SSP5-8.5）ごとに
流域全体（4,381メッシュ）の土地利用構成と流出係数を計算した結果:

| 年 | シナリオ | 流出係数f | 市街地% | 森林% | 耕地% | 草地(粗放化)% |
|---|---|---|---|---|---|---|
| 2020 | 基準/SSP1/SSP5（起点、差なし） | 0.4584 | 34.70 | 59.06 | 1.74 | 4.50 |
| 2030 | 基準 | 0.4594 | 34.84 | 56.76 | 1.64 | 6.76 |
| 2030 | SSP1-2.6 | 0.4607 | 35.04 | 56.68 | 1.64 | 6.65 |
| 2030 | SSP5-8.5 | 0.4694 | 36.32 | 55.67 | 1.64 | 6.38 |
| 2040 | 基準 | 0.4597 | 34.90 | 55.10 | 1.54 | 8.46 |
| 2040 | SSP1-2.6 | 0.4623 | 35.27 | 54.95 | 1.54 | 8.24 |
| 2040 | SSP5-8.5 | 0.4794 | 37.80 | 52.87 | 1.55 | 7.79 |
| **2050** | 基準 | 0.4599 | 34.92 | 53.97 | 1.45 | 9.66 |
| **2050** | **SSP1-2.6** | **0.4636** | **35.45** | **53.73** | **1.46** | **9.36** |
| **2050** | **SSP5-8.5** | **0.4890** | **39.21** | **50.52** | **1.47** | **8.81** |

（全時点の全シナリオはダッシュボード上で対象年スライダー・シナリオボタンを操作すれば再現できる。
上表は検証のため `computeScenario()` のロジックをNode.jsで直接呼び出し全時点を一括計算したもの）

**読み取れること**:
- **市街地率は SSP5-8.5 の方が SSP1-2.6 より一貫して高く伸びる**（2050年時点 39.21% vs 35.45%、
  基準の34.92%との差は+4.3ポイント vs +0.5ポイント）。これは日本版SSPマニュアルの
  「SSP5=大都市圏集中・SSP1=コンパクト+ネットワーク」という定性的なシナリオ設定と整合する。
- **森林率はどのシナリオでも一貫して減少**するが、減少幅はSSP5の方が大きい（59.06%→50.52%）。
  基準シナリオでも59.06%→53.97%と減少しており、**人口減少下での森林管理放棄は
  「高成長シナリオでなくても起きる」**という含意がある。
- 草地（粗放化＝放棄の兆候）は2020年の4.50%から2050年には8.81〜9.66%まで増える。
  興味深いことに**SSP5（市街地増加が大きいシナリオ）の方が草地の増加幅はやや小さい**
  （8.81% vs 基準9.66%）。これは市街地化が進む分、放棄前に転用されてしまうメッシュが
  増えるため（§4 のモデル構造上の理由）。

---

## 3. 🔴 現在の手法の限界 — 「比較静学」であって「経路依存シミュレーション」ではない

### 3.1 何が起きているか（実装の実態）

`computeScenario(yearIdx, scenario, sensitivity)` 関数は、**呼ばれるたびに、常に
2022年のJAXA土地被覆（`f0_2022`・`urban_pct`・`crop_pct`等）を出発点として**計算する。

```
現在の実装(擬似コード):
  for 各年Y in [2020, 2025, ..., 2050]:
      土地利用(Y) = f( 土地利用_2022年時点(固定) , 人口指数(Y)/人口指数(2020) )
                                                  ↑ここだけが年によって変わる

本来の時系列シミュレーションであれば:
  土地利用(2020) = 土地利用_2022年時点
  土地利用(2025) = f( 土地利用(2020) , 2020→2025の人口変化 )   ← 前の年の状態を引き継ぐ
  土地利用(2030) = f( 土地利用(2025) , 2025→2030の人口変化 )   ← 引き継ぐ
  ...
```

### 3.2 これが実質的に何を意味するか

- **年ごとの数値は「もしその年の人口水準まで一気に到達していたら」という仮想的な断面**であり、
  「2022年から段階的にそうなっていく過程」を追跡した結果ではない。
  結果として §2 の表が滑らかに見えるのは、**入力（人口指数）が単調に推移しているため**であり、
  モデル自体が時間的な整合性を保証しているわけではない。
- 🔴 **開発（市街地化）が可逆になっている。** 現実には、一度市街地化した土地が自然に森林や
  耕地へ戻ることはない（不可逆）。しかし現在の実装は毎回2022年の状態から計算し直すため、
  **人口指数が仮に途中で減少に転じた場合、市街地率も"逆戻り"して表示される**。
  今回のデータでは人口指数がほぼ単調に推移するため実害は出ていないが、これは
  **モデルの構造的な弱点であり、偶然目立っていないだけ**という点を明記しておく。
- **「対象年スライダーを動かす」体験は、時系列アニメーションのように見えるが、
  実態は「年ごとに独立した断面図を並べて見ている」に近い。** 誤解を招きやすいため、
  ダッシュボードのUI説明文にこの限界を追記することを推奨する（§6 タスク）。

### 3.3 なぜこの設計にしたか（言い訳ではなく、実装判断の記録）

ハッカソン期限（8/30）と、[[projects/ABM事業化/02-実装設計]] で定めた二層構造
（層A=LLMエージェントの意思決定、層B=決定論的な空間更新）の設計方針に基づき、
**現在完成しているのは層Bの「ある時点の断面を計算する関数」まで**である。
層Aを未実装のまま「経路依存性」を無理に作り込むと、恣意的な仮定（毎年の変化率をどう配分するか等）
が増え、**MEMORY.mdの lesson（もっともらしい表を検証せずに書いた事故）と同種のリスク**になる。
→ 現時点では「正直に断面図であることを明示する」方を選んだ。

---

## 4. 土地利用変化の計算式（現行・層Bの中身）

### 4.1 人口指数からの圧力への変換

```
人口指数 P(mesh, year, scenario)   ← 2020年 = 1.0 の指数（実測 or 地域区分近似、D-013参照）

growth = P(mesh, year, scenario) - 1.0

開発圧%   = clamp( max(0,  growth) × 感度 × 100 ,  0, 60 )
放棄圧%   = clamp( max(0, -growth) × 感度 × 100 ,  0, 60 )
```

「感度」は較正されていない自由パラメータ（デフォルト1.0×）。人口が10%増えたメッシュに
感度1.0×を適用すると、開発圧10%（＝耕地・森林・草地プールの10%を市街地に転用）という設定。

### 4.2 土地利用構成の更新（2022年断面に対して1回で適用）

```
① 管理放棄: (耕地 + 森林) の [放棄圧%] を 草地 へ移動
② 開発:    (耕地 + 森林 + 草地) の [開発圧%] を 市街地 へ移動
   ※①→②の順で適用(放棄後の草地も開発対象プールに含める)
```

### 4.3 流出係数の再計算

移動後の構成比に、平成16年国交省告示第521号ベースの係数（宅地0.90／耕地・林地・原野0.20／
水域1.00）を面積加重平均して f を算出（[[projects/ABM事業化/02-実装設計]] §1 参照）。

---

## 5. ✅ 拡張の実施状況（案A・案B）

### 案A: 逐次更新（実装・検証済み、2026-08-05）

```
土地利用(t) = f( 土地利用(t-1) , 人口指数(t-1)→(t)の変化 )
```

[[projects/ABM事業化/04-流出係数プロトタイプ]] に「累積モード」として実装した。
5年刻みで前ステップの土地利用構成を引き継いで更新し、**市街地化は不可逆**
（`urban` を減少させる操作を行わない）。1ステップあたりの変化上限（デフォルト25%）を
追加し、小メッシュの人口急変（1世帯の転出等）が極端な結果を生まないよう歯止めをかけた。

**検証（Node.jsで全数チェック済み）**: 全4,381メッシュ×全期間（26,286チェック）で、
市街地面積が前の期より減少したケースは**0件**。不可逆性が設計通り成立していることを確認。

### 案B: LLMエージェント層（アーキテクチャ実装済み、実行はMOCK）

[[projects/ABM事業化/06-案B設計と限界]] に分離して記録。要点:
- 開発事業者・地権者の2種のエージェントを、市区町村×地域区分単位（メッシュ単位ではない）で配置。
- 実際に使えるプロンプトテンプレート・API接続点まで実装済み。
- 🔴 **このセッションの実行環境にLLM API認証情報が無いため、判断ロジックは
  ルールベースのMOCK関数で代替して動作確認**（`06_agent_layer_prototype.py`）。
  本物のLLM呼び出しへの切替方法もコード内に明記済み。

### 実施した順序
Shonadeshikoの指示通り「案A→案B」の順で対応。案Aは層Bの範囲内で完結する決定論的な拡張のため
即座に実装・検証まで完了。案Bはコスト・時間の制約（LLM呼び出しの実行環境が無い）から、
アーキテクチャの実装と動作確認（MOCK）までに留めた。

---

## 6. タスク

- [x] ダッシュボードに断面/累積モードの切替と説明文を追加 — **2026-08-05完了**
- [x] 案A（逐次更新・不可逆な市街地化）を実装 — **2026-08-05完了・全数検証済み**
- [x] 年次推移をダッシュボード上にミニグラフとして追加 — **2026-08-05完了**（トラジェクトリーチャート、
      断面/累積モードで線の形が変わる。現在対象年を点線で表示）
- [ ] [P1|S] 案B（LLMエージェント層）の本物のLLM呼び出しを有効化するか判断
      — [[projects/ABM事業化/06-案B設計と限界]] §5 参照 | 期限: 要相談

## 関連
- [[projects/ABM事業化/02-実装設計]]（層0〜層C構造、流出係数の算出方法）
- [[projects/ABM事業化/scripts/04_integrate_ssp_population.py]]（人口指数の算出方法）
- [[projects/ABM事業化/04-流出係数プロトタイプ]]（本ノートで扱う計算を実装したダッシュボード）
- [[decisions/log]] D-011 / D-012 / D-013
