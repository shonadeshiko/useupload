---
type: project-detail
name: 案B（LLMエージェント層）の設計と限界
parent: [[projects/ABM事業化/05-気候変動シナリオ×土地利用変化-分析手法]]
status: ✅ Ollamaで実際に実行・検証済み（24回のLLM呼び出し、モックではない）
tags: [LLMエージェント, ABM, 案B, 設計, 限界, Ollama]
created: 2026-08-05
updated: 2026-08-05
---

# 案B（LLMエージェント層）の設計と限界

> 2026-08-05、Shonadeshiko の依頼「案Aをやったのち案Bにしてください。両方ともMDを作って
> どのように違うのか、どのような見せ方になるのかも示してください」への回答。
> 本ノートは案B（層A＝LLMエージェント）を扱う。案A（層B内の逐次更新・不可逆化）は
> [[projects/ABM事業化/05-気候変動シナリオ×土地利用変化-分析手法]] §3・§5 参照。

---

## 0. ✅ 更新（2026-08-05）— Ollamaで実際にLLM呼び出しを実行した

当初、このセッションの実行環境にクラウドLLM（Anthropic/OpenAI）のAPI認証情報が無く、
判断ロジックはMOCK（ルールベース代理関数）で代替していた。
Shonadeshiko から「ollamaとかで対応することはできるか」との提案を受け、確認したところ
**このマシンには Ollama が既にインストール・稼働済み**であることが判明した。

**モデル選定の経緯（正直な記録）**:
- 既存導入済みの `qwen3.5:9b`（6.6GB）をまず試したが、**CPU推論のみの環境で
  応答が極端に遅く、180秒待っても完了しない**事例が発生（`ollama ps` で確認したところ
  「100% CPU」＝GPU未使用）。実用的でないと判断し中断。
- Shonadeshiko の「軽いモデルで対応したい」との要望を受け、**`qwen2.5:1.5b`（0.99GB）**
  に切り替え。簡単な挨拶で4.4秒、実際のエージェント判断プロンプト（JSON構造化出力）で
  6.0秒という実用的な速度を確認。

**実行結果**: 開発事業者・地権者の2エージェント × 2地域区分 × 6時点 =
**24回のLLM呼び出しをすべて実際にOllama経由で実行**（1回だけタイムアウトで自動リトライ、
最終的に24/24成功）。合計200.7秒（平均8.4秒/回）。
結果は [[projects/ABM事業化/_agent_layer_ollama_log.json]] に保存済み（`mock: false` フラグ付き）。

→ **これは本物のLLM推論であり、モックではない。** ただし §3-2 で正直に報告する通り、
1.5Bという小規模モデルゆえの限界（数値コンテキストとの相関の弱さ）も観測された。

---

## 1. 案A と案B の違い（設計思想）

| | 案A（逐次更新・不可逆化） | 案B（LLMエージェント層） |
|---|---|---|
| 経路依存性の出どころ | **状態を引き継ぐ計算式**（前年の土地利用を次のステップの入力にする） | **エージェントの意思決定**（前の状況を見て次の判断をするため、自然に経路依存になる） |
| 判断の中身 | 人口指数の増減 → 固定の数式（線形変換＋上限）で dev%/abn% を決める | 人口動向・市街地化率・残存率等の**複数の文脈情報を統合**し、
質的な理由づけとともに intensity を決める |
| 恣意性の所在 | 数式のパラメータ（感度・上限%）が恣意的 | プロンプトの設計・エージェントの人格設定が恣意的（+ LLMの確率的なブレ） |
| 実行コスト | ほぼゼロ（ブラウザ内で即時計算） | LLM API呼び出しのコスト・時間（エージェント数×時点数×シナリオ数） |
| 説明可能性 | 数式なので**完全に再現可能**。「なぜこの値か」は数式を読めば分かる | **意思決定の理由（reasoning）がテキストで得られる**。
「なぜこの地域は開発されたか」を自然文で説明できる。ただし再実行すると理由文が変わりうる（LLMの非決定性） |
| 実装状況（2026-08-05） | ✅ **実装・検証済み**（[[projects/ABM事業化/04-流出係数プロトタイプ]] の「累積モード」） | ✅ **Ollama(`qwen2.5:1.5b`)で実際に24回のLLM呼び出しを実行・検証済み**
（[[projects/ABM事業化/scripts/06_agent_layer_prototype.py]]、まだ層Bへの接続は未実施） |

**要するに**: 案Aは「同じ計算式を、時間をかけて積み上げる」。案Bは
「積み上げる主体（エージェント）自体に判断力を持たせる」。**両者は排他ではなく重ねられる**
（§4 参照。案Bのエージェント判断は、案Aの逐次更新の中の devPct/abnPct を補正する形で接続する）。

---

## 2. 案Bのアーキテクチャ（実装済み・実在するコード）

### 2.1 エージェント設計 — メッシュ単位ではなく「市区町村×地域区分」単位

🔴 4,381メッシュ全部にLLMを当てると、呼び出し回数（メッシュ数×時点数×シナリオ数）が
膨大になりコスト・時間の両面で破綻する（[[projects/ABM事業化/02-実装設計]] §2.1で
既に指摘済みの設計原則）。そこで [[projects/ABM事業化/scripts/04_integrate_ssp_population.py]]
で既に構築した**地域区分（A=大都市圏市街地／B=大都市圏非市街地）**をそのまま流用し、
**地域区分ごとに代表エージェントを1体ずつ**置く。

```
4,381メッシュ                  2つの地域区分(A/B)              2種のエージェント
  ├─ 個別のメッシュには       ├─ A: 大都市圏市街地      ├─ 開発事業者エージェント
  │  LLMを当てない            ├─ B: 大都市圏非市街地     └─ 地権者(集落)エージェント
  └─ 地域区分を継承して        
     エージェントの判断を
     受け取る
```

### 2.2 2種類のエージェント

| エージェント        | 役割               | 出力                           |
| ------------- | ---------------- | ---------------------------- |
| **開発事業者**     | その地域区分の開発意欲を判断   | intensity(0-100) + reasoning |
| **地権者(集落代表)** | その地域区分の管理放棄意欲を判断 | intensity(0-100) + reasoning |

プロンプトは実在する完成品（[[projects/ABM事業化/scripts/06_agent_layer_prototype.py]] の
`DEVELOPER_AGENT_PROMPT` / `LANDOWNER_AGENT_PROMPT`）。人口動向・市街地化率・残存率などの
数値コンテキストを埋め込み、JSON形式での回答を要求する設計。**そのままAPIに投げられる**。

### 2.3 層Bとの接続点（設計）

```
層A(エージェント)の出力: intensity(0-100)
              ↓ 100で正規化して乗数化
層B(案Aの逐次更新式)の devPct / abnPct に掛け算
              ↓
devPct_最終 = devPct_機械式(人口指数由来) × (intensity_developer / 50)
abnPct_最終 = abnPct_機械式(人口指数由来) × (intensity_landowner / 50)
```

intensity=50を「機械式のまま」の基準点とし、50超なら加速、50未満なら抑制する設計
（🔴 この正規化の仕方も含めて未実装・要検討。§5 タスク参照）。

---

## 3. 実行結果（✅ Ollama実行・本物のLLM推論、2026-08-05）

`06_agent_layer_prototype.py` を `use_ollama=True` で実行し、地域区分A/B ×
2025〜2050年の6時点でエージェント判断のログを取得した
（[[projects/ABM事業化/_agent_layer_ollama_log.json]]、`mock: false`）。一部抜粋（実際の出力そのまま）:

```
[2025年 / A_大都市圏市街地]
  開発事業者エージェント: intensity=75 (6.1秒)
    理由: この地域は多摩川流域にある大都市圏市街地であり、人口が増加している一方で、
    地価や生態系サービス価値の動向がデモ範囲外です。この2つの要素から、現行の開発強度
    35.0%から少し向上した状況を見込んでいます。

[2050年 / B_大都市圏非市街地]
  地権者エージェント:     intensity=85 (5.4秒)
    理由: この地域は大都市圏非市街地で、人口が趨勢的に増加している。これは継続的な
    開発需要のため、農地の使用形態が変化する可能性がある。現在60.0%の耕地残存率を
    考えると、一部の土地は放置される可能性があると考えられる。また、近隣に高い開発
    が発生するため、地域全体の管理放棄意欲が低下していると判断できます。
```

これは**モックではない、本物のLLM推論の出力**。全24回の呼び出しがOllama経由で実行され、
成功している（1回タイムアウトで自動リトライ、最終的に全成功）。

### 3-1. 良かった点
- **文脈に応じた理由づけが得られる。** 「人口動向」「市街地化率」「耕地残存率」という
  数値だけでなく、「開発需要」「管理放棄意欲」といった**質的な概念**を使った説明が
  自然に生成される。これはMOCKの定型文には無い価値。
- **1.5Bという小型モデルでも実用的な速度**（平均8.4秒/回）で、JSON構造化出力も
  安定して得られた（24回中フォーマットエラーは0件、タイムアウトのみ1回でリトライ成功）。

### 3-2. 🔴 正直に報告すべき限界 — 数値コンテキストとの相関が弱い

`A_大都市圏市街地` の人口動向は 2025→2050年にかけて **+1.9% → +0.9%（一貫して減速する
プラス成長、マイナスには一度も転じない）** という単調な入力だったが、
LLMが返した開発強度スコアは **75, 75, 75, 50, 60, 50** と、単調減少にはならず、
2045年に60へ跳ね上がるなど**非単調な挙動**を見せた。地権者エージェントの放棄強度も
同様に **75, 75, 55, 45, 45, 65** と、2050年に人口がまだプラス成長（+0.9%）にも
関わらず放棄強度が上昇するという、**やや一貫性を欠く判断**が観測された。
`B_大都市圏非市街地` でも同様の傾向（放棄強度が最終的に85という高スコアに達するが、
人口動向は最後まで一度もマイナスに転じていない）。

**解釈**: 1.5Bパラメータという小規模モデルは、数値入力を厳密に一貫したロジックで
処理するというよりも、**プロンプト内の他の定性的な記述（地域特性の説明文等）に
引きずられてスコアがブレる**傾向があると考えられる。これは捏造ではなく、
**実際に起きた観測結果**であり、正直に報告する。
より大きなモデル（9B以上、または `format:json` 非対応でも思考過程を出力できるモデル）
であれば改善する可能性があるが、**それは検証していない推測**にとどめる。

### 3-3. 実務上の含意
- 現時点のスコアを**そのまま層Bの乗数として使うのは時期尚早**。まず
  「同じ入力に対して複数回実行し、スコアのばらつきを見る」という頑健性チェックが必要
  （§5 タスク）。
- ただし「reasoning（理由文）」の質は高く、**説明可能性（なぜこの判断か）を人間に
  提示する**という目的においては、intensityスコアの数値精度より reasoning の
  質の方が現時点では価値が高いかもしれない。

---

## 4. 見せ方の違い（プレゼンテーション設計）

### 案A（[[projects/ABM事業化/04-流出係数プロトタイプ]] に実装済み）
- **地図＋数値ダッシュボード**が中心。「断面モード」「累積モード」のトグル。
- 「断面 vs 累積の差分」テーブルで、2つの計算方式の違いを**数値で**見せる。
- 訴求点: **再現性・透明性**。「このパラメータでこの結果になる」を誰でも検証できる。

### 案B（想定・未実装のUI）
- 地図・数値に加えて、**エージェントの「発言ログ」パネル**を追加する想定。
  時点を送ると、その時点でどのエージェントが何を判断し、なぜそう判断したかが
  自然文で流れる（チャットログ風のUI）。
- 訴求点: **説明可能性・物語性**。「なぜこの地域が開発されたのか」を
  人間が読める理由とともに提示できる。審査基準①「問いの面白さ」に直接効く。
- 🔴 未実装。ハッカソンに含めるかはコスト（API呼び出し回数・実行時間）との兼ね合いで
  Shonadeshikoの判断が必要（§5 タスク）。

### 両方を重ねた場合の見せ方（理想形・未実装）
```
┌─ 地図(案A・数値ベース) ──────────────┐
│  流出係数マップ・断面/累積モード切替       │
└──────────────────┬──────────────────┘
                     │ 連動
┌─ エージェント発言ログ(案B) ────────────┐
│  [2050年] 開発事業者「人口が増えているので │
│  この地域への投資を強めます」              │
│  地権者「後継者不足で管理を続けられません」 │
└─────────────────────────────────────┘
```
数値（案A）と物語（案B）を並置することで、「モデルが何を計算したか」と
「なぜそうなったと解釈できるか」の両方を提示できる。これは
[[projects/ABM事業化/01-ハッカソン応募案]] の審査基準②「連鎖・創発が観測できたこと」
に対する最も説得力のある見せ方になりうる。

---

## 5. 🔴 未解決・要判断

- [x] ~~本物のLLM呼び出しを有効化するか~~ — **2026-08-05 Ollamaで実行・解決**。
      クラウドAPI不要、費用ゼロで24回のLLM呼び出しを実行済み。
- [ ] [P0|S] **intensityスコアの頑健性チェック**（同一入力での複数回実行によるばらつき確認）
      — Why: §3-2で観測した非単調な挙動が、モデルの本質的なノイズか偶然かを見極める必要
      | 期限: 08/08
- [ ] [P1|S] より大きめのローカルモデル（例: `qwen2.5:7b`等、CPU速度と要相談）で
      同じ実験をして§3-2の非単調性が改善するか比較する — Why: 1.5Bの限界か
      プロンプト設計の問題かの切り分け | 期限: 08/10
- [ ] [P1|S] intensity(0-100)を層Bのdevpct/abnPctへ掛け合わせる**正規化方法**を確定する
      — Why: §2.3の「50を基準点にする」は仮の設計。§3-2の頑健性チェック後に着手 | 期限: 08/12
- [ ] [P1|S] エージェント発言ログのUIパネルを実装するか判断 — Why: 訴求力は高いが
      実装コストが増える（API費用は既にゼロ化されたため、コスト面の障壁は下がった） | 期限: 要相談
- [ ] [P2|S] エージェント単位を「市区町村×地域区分」からさらに細かく
      （例: 市区町村単体、〜30体）にするかの検討 — Why: 地域区分2種類では
      流域内の多様性を十分表現できていない可能性。Ollamaなら呼び出し増でも費用は増えない
      （時間のみ増える） | 期限: 08/17

## 関連
- [[projects/ABM事業化/02-実装設計]]（層A/層Bの当初設計）
- [[projects/ABM事業化/05-気候変動シナリオ×土地利用変化-分析手法]]（案Aの詳細）
- [[projects/ABM事業化/scripts/06_agent_layer_prototype.py]]（本ノートの実装）
- [[projects/ABM事業化/04-流出係数プロトタイプ]]（案Aが実装されているダッシュボード）
- [[decisions/log]] D-011 / D-012 / D-013
