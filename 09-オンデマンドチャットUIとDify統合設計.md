---
type: project-detail
name: オンデマンドチャットUIの設計と、Web製品化に向けたDify統合の道筋
parent: [[projects/ABM事業化/08-地図データ直結型エージェントUI]]
status: チャットUIは実装・検証済み。Dify統合は設計のみ（未実装）
tags: [LLMエージェント, チャットボット, Ollama, Dify, Web製品化, UI]
created: 2026-08-05
updated: 2026-08-05
---

# オンデマンドチャットUIの設計と、Web製品化に向けたDify統合の道筋

> 2026-08-05、Shonadeshiko の質問「オンデマンドでやるとしたらどのようになるか。
> AIをこのWeb上で対応できるような設計にしておきたい。Difyのようなチャットボットが
> このWeb上で画面のパラメータなどを学習してレスポンスが出せるとよい」への回答。
> 「Defy」は文脈から**Dify**（オープンソースのLLMアプリ構築プラットフォーム）の
> こととして扱う。ここは断定できないため、違っていれば訂正してほしい。

---

## 0. 結論（先に2点）

1. **「画面のパラメータを学習してレスポンスを出す」チャットUIは、Difyを使わなくても
   今すぐ実装・動作確認できた。** [[projects/ABM事業化/04-流出係数プロトタイプ]] に
   実装済み。地図の選択メッシュ・モード・年・シナリオ・集計値など、**画面に表示されている
   すべての値を毎回自動でAIの文脈に注入**する設計にした。
2. **Dify自体は実在し、Ollamaとの連携も、Webサイトへの埋め込みも標準機能として持っている。**
   ただし「Web製品として不特定多数に公開する」場合、**Difyをホストするサーバーが必要**
   になる（現在の「1ファイルの静的HTML＋閲覧者自身のOllama」という構成とは前提が変わる）。
   §3 で詳細と選択肢を示す。

---

## 1. 実装したオンデマンドチャットUI

### 1-1. 「学習」の中身 — 毎回のメッセージに画面状態を自動添付する

Difyの用語で言う「変数(inputs)」に近い仕組みを、Ollamaの`/api/chat`（会話API）を使って
自前で実装した。ユーザーが質問を送るたびに、**その時点の画面状態をシステムメッセージとして
自動生成し、質問の前に挿入**する。

```
ユーザーが「送信」を押すたびに実行される処理:

  1. 画面の現在値を集計する
     ├─ モード（断面／累積）・対象年・人口シナリオ・感度・降雨倍率
     ├─ 流域全体の集計（平均流出係数・高リスクメッシュ数・成長/減少メッシュ数）
     └─ 選択中のメッシュがあれば、その実際の値（f・人口指数・土地利用構成）
              ↓
  2. これを日本語のシステムメッセージ文字列に組み立てる（buildSystemContext関数）
              ↓
  3. [システムメッセージ, ...直近の会話履歴, 新しい質問] を
     Ollamaの /api/chat エンドポイントに渡す
              ↓
  4. 応答をチャットログに追加。会話履歴として保持（次の質問でも文脈が続く）
```

**これはモデルの「学習」（ファインチューニング）ではなく、毎回のプロンプトに
最新の画面状態を差し込む「プロンプトインジェクション（文脈付与）」である。**
[[projects/ABM事業化/07-頑健性検証と本番化への道筋]] で確認した通り、この1.5Bモデルを
ファインチューニングするには訓練データ・GPU・数日単位の時間が要り現実的でない。
一方、この「画面状態を文脈に含める」方式なら学習は一切不要で、**画面が変われば
（スライダーを動かせば）次の質問から自動的に新しい状況を踏まえた回答になる**。

### 1-2. 検証（実データ・実コードで確認済み、2026-08-05）

Node.jsの`fetch()`で、実際にダッシュボードと同じ画面状態（累積モード・2050年・SSP5-8.5・
流域平均f=0.487・f≥0.7のメッシュ1,453件）をシステムメッセージとして注入し、
「今の設定だと、流域全体としてはどういう状況ですか」と質問したところ:

> 現在の設定では、流域全体の流出係数が0.487であり、f≥0.7のメッシュ数は1453です。
> これは一定以上の流出係数を持つ地域が広範囲にあることを示しています。
> 人口成長と減少のメッシュ数も大きく異なります。

**画面に表示されている実際の数値（0.487、1453）をそのまま参照した回答**が得られた。
新しい数値を捏造していないことも確認済み。

### 1-3. UI（実装済み）

- チャットログ（複数ターンの会話を表示）
- テキスト入力欄＋送信ボタン（Enterキーでも送信可）
- 会話クリアボタン
- 地図クリックでメッシュを選択すると、そのメッシュの情報も自動的に文脈へ追加される
  （[[projects/ABM事業化/08-地図データ直結型エージェントUI]] の仕組みをチャット化して継承）

---

## 2. Dify とは（一次確認・2026-08-05）

Shonadeshikoの言う「Defy」はDifyのことと解釈して調査した。実在するオープンソースの
LLMアプリ構築プラットフォームで、以下を確認済み:

- **ノーコードでチャットボット・ワークフローを構築できる**（プログラミング知識不要）
- **Ollamaとの連携が標準機能**: OllamaのURLを指定するだけで、自前ホストのモデルを
  Difyのモデルプロバイダーとして使える
- **Web埋め込みが3通り**: ①iframe埋め込み、②スクリプトタグでのフローティングウィジェット、
  ③REST API（`/v1/chat-messages`）でのカスタムUI構築
- **`inputs`パラメータで動的変数を渡せる**: APIの`chat-messages`エンドポイントは
  `query`（質問文）とは別に`inputs`オブジェクトを受け取り、Dify側のプロンプトテンプレートに
  `{{変数名}}`として埋め込める。これが「画面のパラメータを渡す」仕組みに直接使える。

出典: [DifyとOllamaを連携する方法（Dify公式パートナー記事）](https://dify.tdse.jp/post_column/359/) /
[こんなに簡単 DifyでカスタマーサポートAIチャットbotをWebサイトに組み込む方法](https://atmarkit.itmedia.co.jp/ait/articles/2507/17/news001.html) /
[DifyのWebフロント作成を実践](https://data-insight.biz/blog/2026-02-02-dify-05-webfront-guide.html) /
[Dify APIを使う際の注意点](https://qiita.com/firesign2023/items/941edb7e933cd845fb48)

---

## 3. 今の自前実装 vs Dify — どちらを選ぶか

| | 今の自前実装（§1、実装済み） | Dify統合（未実装・設計のみ） |
|---|---|---|
| 追加インフラ | **不要**（1ファイルの静的HTML＋閲覧者のOllama） | **必要**（Difyサーバーのホスティング、Docker等） |
| 画面パラメータの注入 | 自前のJS関数（`buildSystemContext`）で実装済み | Dify APIの`inputs`パラメータで同等のことが可能（未実装） |
| 会話管理 | 自前のJS配列（ブラウザのメモリ内、リロードで消える） | Difyが会話ID・履歴を管理（DBに永続化される） |
| プロンプトの調整 | コードを書き換える必要がある | **Difyの画面上でノーコードで調整できる**（本人が今後いじりやすい） |
| RAG（文献・過去ログを参照） | 無し | **Difyの標準機能**。本vaultのMDファイル群を
知識ベースとして取り込める可能性がある |
| モデルの切り替え | コードでURL/モデル名を変更 | Dify画面上でモデルプロバイダーを切り替えるだけ |
| Web公開のしやすさ | 静的ファイルなのでどこにでも置けるが、
**閲覧者自身のOllamaが必要**（[[projects/ABM事業化/08-地図データ直結型エージェントUI]] §4） | サーバーさえ用意すれば、**閲覧者はOllama不要**
（Dify側がLLM推論を代行する） |
| ハッカソン(8/30)に間に合うか | ✅ 済 | 🔴 サーバー構築・動作確認の時間が要る。要検討 |

### 🎯 推奨

- **ハッカソン提出**: §1の自前実装をそのまま使う。追加インフラが要らず、既に動作確認済み。
- **その先（事業化・継続開発を見据える場合）**: Difyへの移行を検討する価値がある。
  理由は「プロンプトをノーコードで調整できる」「会話ログが残る」「将来的にRAGで
  vault内の文献・決定ログを参照させられる」の3点。特に最後は
  `context/about-me.md`の「webGISなども活用して外部のオープンソースの媒体として提供」
  という方針、および[[projects/ABM事業化/00-事業フレーム]]の事業化構想と接続する。

---

## 4. Dify統合の具体的な道筋（設計・未実装）

実装するとしたら、という前提で手順を示す。

### 4-1. サーバー構成

```
選択肢A: 自宅/オフィスのマシンにDify+Ollamaを両方載せる
  → 追加費用ゼロだが、そのマシンが起動していないとWebサイトが機能しない

選択肢B: クラウドVPS(例: さくらのVPS、AWS Lightsail等)にDify+Ollamaを載せる
  → 月額費用が発生する。CPU推論なら比較的安価なVPSでも動く
    （今回 qwen2.5:1.5b は普通のノートPCのCPUで4〜10秒/回で動作した実績あり）

選択肢C: Difyはクラウド版(dify.ai公式のSaaS)を使い、モデルはクラウドAPI(Anthropic等)に戻す
  → サーバー構築は不要だが、D-014で一度離脱したクラウドAPI課金が復活する
```

**推奨は選択肢B。** ローカルOllamaで実証済みの低コスト性（無料・CPUで十分）を維持しつつ、
Web公開に必要な「閲覧者側にOllama不要」を実現できる。

### 4-2. Difyアプリの構築手順（概要）

1. Dify（Docker Compose、公式配布）をVPSに導入
2. モデルプロバイダーにOllamaを追加（VPS上のOllamaのURLを指定）
3. Difyの「チャットボット」アプリを新規作成
4. プロンプトテンプレートに`{{mode}}` `{{year}}` `{{scenario}}` `{{mean_f}}`
   `{{selected_mesh_info}}`等の変数プレースホルダを設定（§1のbuildSystemContextの
   内容をDifyの変数定義に移植する）
5. Webサイト（04ダッシュボード）側のJSを、Ollama直接fetchから
   Dify APIの`/v1/chat-messages`呼び出しに差し替え、`inputs`に画面状態を渡す

### 4-3. 🔴 未解決・要検討

- [ ] VPSの費用感（月額）を調査する — Why: 「無料」から「小額課金」への切り替えになるため、
      事業化の採算とセットで検討すべき
- [ ] Dify自体のセキュリティ・アクセス制御（誰でも使えると費用が青天井になりうる）
- [ ] RAG機能でvault内のMDファイル（decisions/log.md等）をどこまで参照させるか
      — 客先情報・個人情報を含むファイルを誤って学習データに含めないよう選別が必要

---

## 5. タスク

- [x] オンデマンドチャットUIの実装（画面状態の自動注入・複数ターン会話） — **2026-08-05完了**
- [x] 実データでの動作検証（Node.js `fetch()`で画面状態注入→正しい参照回答を確認） — **完了**
- [ ] [P2|M] Dify統合を実際に試すか判断 — Why: ハッカソンには不要だが、事業化を見据えるなら
      早めに触っておく価値がある | 期限: ハッカソン後・要相談
- [ ] [P3|S] VPSの費用感を調査 — Why: Dify統合を検討する前提条件 | 期限: 未定

## 関連
- [[projects/ABM事業化/04-流出係数プロトタイプ]]（チャットUIを実装したダッシュボード本体）
- [[projects/ABM事業化/08-地図データ直結型エージェントUI]]（本UIの前身・単発Q&A版）
- [[projects/ABM事業化/07-頑健性検証と本番化への道筋]]（ファインチューニングとの比較）
- [[projects/ABM事業化/00-事業フレーム]]（Web公開・事業化の方針）
- [[context/about-me]]
- [[decisions/log]] D-016 / D-017
