---
type: project-detail
name: 案Bの頑健性検証・強化学習に必要なこと・Web公開への道筋
parent: [[projects/ABM事業化/06-案B設計と限界]]
status: 頑健性検証は完了。学習・Web公開は設計・未実装
tags: [LLMエージェント, 頑健性, ファインチューニング, Web公開, Ollama]
created: 2026-08-05
updated: 2026-08-05
---

# 案Bの頑健性検証・強化学習に必要なこと・Web公開への道筋

> 2026-08-05、Shonadeshiko の依頼「頑健性をチェックしてほしい。強化して学習するには何が
> 必要か。Webプロダクトで動かすにはどうすればいいか」への回答。3つの問いに順に答える。

---

## 1. 頑健性チェックの結果（実測）

[[projects/ABM事業化/scripts/07_robustness_check.py]] で、同一入力を5回ずつ投げて
intensityスコアのばらつきを測定した（`qwen2.5:1.5b`、2026-08-05実施）。

### 1-1. デフォルト設定 vs temperature=0

| 条件 | 平均 | 標準偏差 | 範囲 |
|---|---|---|---|
| 開発事業者・デフォルトtemperature | 57.0 | **11.2** | 45〜75 |
| 開発事業者・temperature=0 | 50.0 | **0.0** | 50〜50 |
| 地権者・デフォルトtemperature | 39.0 | **12.0** | 25〜55 |
| 地権者・temperature=0 | 50.0 | **0.0** | 50〜50 |

**デフォルト設定は同じ入力でも標準偏差11〜12という大きなばらつきを見せる。**
これでは「同じ状況なら同じ判断をする」というモデルの基本要件を満たさない。

**temperature=0（貪欲デコーディング）にすると完全に決定論的（標準偏差0）になる。**

### 1-2. 🔴 ただし temperature=0 には別の弱点がある — 細かい差に鈍感

temperature=0 のまま、入力の大きさを変えて追加検証したところ:

| 入力 | intensity(temp=0) |
|---|---|
| 人口動向 +30.0%・市街地化率10.0%（極端な成長） | **75** |
| 人口動向 +0.9%・市街地化率35.0%（本番で使う値、穏やか） | **50** |
| 人口動向 −30.0%・市街地化率90.0%（極端な減少） | **25** |

→ **極端な入力には正しく反応する**（75→50→25と単調に変化）。文脈を無視して
常に固定値を返しているわけではないことが確認できた。
→ 🔴 **しかし本番で実際に使う「穏やかな差」（+1.9%〜+0.9%程度の変化）には
反応が鈍く、6時点すべてで同じ値（50）に収束してしまう可能性が高い。**

**解釈**: 1.5Bモデルは「大づかみな方向感」は正しく持っているが、
「人口動向が+1.9%か+0.9%か」のような**繊細な数値の違いを継続的な尺度として
使いこなす訓練を受けていない**（そもそも汎用対話用に訓練されたモデルであり、
本タスクのような数値回帰的な判断に特化していない）。

### 1-3. もう1つの発見 — 言語ドリフト（対応済み）

temperature=0 での検証中、**プロンプトが日本語なのに理由文が中国語で返ってくる**
現象が複数回発生した（`qwen2.5` はAlibaba製の多言語モデルのため）。
プロンプトに「理由は必ず日本語で書いてください」を明記したところ解消し、
副次的に判断の一貫性も改善した（地権者の温和な入力に対する応答が50→25等、
より状況に応じた値になった）。→ **[[projects/ABM事業化/scripts/06_agent_layer_prototype.py]]
のプロンプトに反映済み。**

### 1-4. 結論・当面の対応

- ✅ **`temperature=0` をデフォルトにする**（06のコードに反映済み）。
  再現性を最優先する（同じ状況で毎回違う判断をするモデルは信頼できない）。
- ✅ **日本語指定をプロンプトに追加**（反映済み）。
- 🔴 **ただし「穏やかな入力の違いに反応しない」問題は未解決。** 現状のまま
  intensityスコアを層Bの乗数として使うと、**6時点で同じ値になり、案Bを入れても
  案Aと変わらない結果になる可能性が高い**。§2 の対応が必要。

---

## 2. 強化学習・ファインチューニングに必要なこと

Shonadeshiko の質問「強化して学習するにはどうすればいいか」への回答。
**結論から言うと、3つの選択肢があり、コストと効果が大きく異なる。**

### 選択肢A（★推奨・すぐできる・学習不要）: LLMを「数値の決定」から外し「説明」だけに使う

```
現状の設計（LLMが数値も理由も両方決める）:
  LLM ← プロンプト（人口動向・市街地化率等）
  LLM → intensity(数値) + reasoning(理由文)
              ↑ ここが鈍感・不安定（§1の問題）

推奨する設計（数値は機械式、LLMは理由文だけを書く）:
  層B(案A)の機械式 → devPct/abnPct(数値、既に感度良く計算できている)
              ↓ 数値を埋め込んでLLMに渡す
  LLM ← 「devPct=●%と計算されました。なぜそうなったか、地域の文脈を踏まえて
         200字程度で説明してください」
  LLM → reasoning(理由文のみ)
```

- **学習・ファインチューニングが一切不要。** 今すぐ実装できる。
- 数値の精度・再現性は層B（決定論的な数式）がそのまま担保する。
- LLMの役割を「数値の決定」から「決まった数値の説明」に変えるだけで、
  §1で見つかった弱点（鈍感・不安定）を回避しつつ、案Bの本来の価値
  （説明可能性・物語性）は維持できる。
- 🔴 ただし「エージェントが自律的に意思決定している」という感覚は弱まる
  （層Bの数式が主役になる）。ハッカソンの「創発」という審査基準への
  訴求力は選択肢B・Cより弱いかもしれない。

### 選択肢B: プロンプトエンジニアリングで改善（学習不要、中程度の工数）

- **Few-shotの例を埋め込む**: 「人口動向+2%なら intensity=60、+1%なら55、0%なら40、
  -1%なら25、のように滑らかに変化させてください」という**具体例つきの指示**を
  プロンプトに追加する。今回はこれを試していない。
- **Chain-of-thought方式**: 「まず人口動向を0-100の内部スコアに変換し、次に
  市街地化率で補正し、最後に理由を書いてください」と、計算過程を明示的に
  ステップ化させる。
- 🔴 効果は未検証。試す価値はあるが、**小型モデルでは効果が限定的な可能性**
  （プロンプトの指示に従う能力自体がモデルサイズに依存するため）。

### 選択肢C: 本格的なファインチューニング（学習が必要、最も本格的）

これが本来の意味での「強化して学習する」。必要なものを正直に列挙する:

| 必要なもの | 内容 |
|---|---|
| **訓練データ** | 「入力(人口動向・市街地化率等) → 正解のintensity」のペアを多数用意する。
正解は**層B(案A)の機械式の出力をそのまま「正解ラベル」として使う**のが現実的
（人間が全部ラベル付けするのは非現実的）。例: devPct機械式の値を0-100に
正規化したものを教師信号にする |
| **ファインチューニング手法** | LoRA（Low-Rank Adaptation）が現実的。
Ollamaは `ollama create` でカスタムモデルを作れるが、**LoRA訓練自体は
別ツール**（Unsloth・Axolotl・Hugging Face PEFT等）で行い、
できたアダプタをOllama用に変換する必要がある |
| **計算資源** | 1.5B〜3B程度のモデルのLoRA訓練なら、GPU（VRAM 8GB程度）があれば
数時間〜半日程度。**このマシンはCPUのみ（GPU未使用、§1参照）のため、
訓練は別環境（クラウドGPU等）が必要になる可能性が高い** |
| **時間** | データ準備・訓練・評価のサイクルで、最低でも数日〜1週間程度 |

🔴 **8/30のハッカソン締切には現実的に間に合わない。** ファインチューニングは
「別研究（[[projects/流域スケール自然資本評価/00-研究構想]]）」の射程に置くのが妥当。

### 推奨する順序

1. **今すぐ**: 選択肢A（LLMを説明役に限定）に切り替える。学習不要、すぐ実装できる。
2. **時間があれば**: 選択肢B（プロンプト改善）を試し、Aと比較する。
3. **ハッカソン後・別研究として**: 選択肢C（本格的なファインチューニング）を検討する。

---

## 3. Webプロダクトで動かすには

Shonadeshiko の質問「Webプロダクトでも動かすにはどうすればいいか」への回答。
**現状のダッシュボード（案A）とLLMエージェント層（案B）で、必要な構成が全く異なる**
ため分けて説明する。

### 3-1. 案A（数値ダッシュボード）— 今のままWeb公開できる

[[projects/ABM事業化/04-流出係数プロトタイプ.html]] は**完全に自己完結した
静的HTML**（データもJSも1ファイルに埋め込み済み、サーバー不要）。

→ **そのままアップロードするだけで公開できる。**
  候補: GitHub Pages / Cloudflare Pages / Vercel（静的サイトモード）/ 単純なS3ホスティング。
  いずれも無料枠で足りる規模（〜1MBのファイル）。
  → `context/about-me.md` の「webGISなども活用して外部のオープンソースの媒体として
  提供できるようにする」という方針とそのまま合致する。

### 3-2. 案B（LLMエージェント層）— 2つの道がある

#### 道1（★推奨）: 事前計算（バッチ）方式 — サーバー不要

```
【裏側で事前に1回だけ実行】
06_agent_layer_prototype.py を手元のOllamaで実行
  → intensity・reasoningのJSON出力（既に実施済み）
              ↓
【Web公開するもの】
04のダッシュボードに、この事前計算済みJSONを埋め込む
（案Aと同じ「1ファイルで完結する静的サイト」のまま）
```

- エージェントは市区町村×地域区分単位（今回はA/Bの2つ）× 時点数なので、
  組み合わせ数が少なく、**事前に全部計算して埋め込んでしまえる**。
- ユーザーがブラウザを開いた時点でLLMは動いていない。**サーバー費用ゼロ、
  Ollamaのホスティング不要**。
- 🔴 欠点: ユーザーが自由にパラメータ（地域区分の粒度・シナリオ設定等）を
  変えて「その場でLLMに聞き直す」ことはできない。あらかじめ計算した
  組み合わせしか見せられない。
- **感度パラメータ・SSPシナリオ・年次は既に事前計算で全パターン用意できる
  規模**（2地域区分×6時点×2種のエージェント=24パターン）なので、
  この方式で十分に「動いているように見える」体験は作れる。

#### 道2: ライブ方式 — サーバーが必要（ユーザーの操作に応じてその場でLLMを呼ぶ）

```
ブラウザ(フロントエンド)
    ↓ ユーザーがパラメータを変更
バックエンドサーバー(要構築・要ホスティング)
    ↓
LLM推論
    ├─ 自前サーバーにOllamaを載せる（GPU付きVPS等、コスト発生）
    └─ または クラウドAPI（Anthropic/OpenAI等）に戻す（呼び出し課金）
```

- **必要になるもの**: ①軽量バックエンド（Flask/FastAPI等）、②LLM推論環境の
  ホスティング（Ollama用のGPU付きサーバー、または クラウドAPI）、
  ③フロントエンドとバックエンドの通信（CORS設定・APIキー管理等）。
- 🔴 **費用と運用の手間が発生する。** 複数ユーザーが同時にアクセスした場合の
  同時実行制御も必要になる（Ollamaは1台のマシンで同時に捌ける推論数に限りがある）。
- ハッカソン規模のデモには過剰。**事業化フェーズ**（[[projects/ABM事業化/00-事業フレーム]]）
  まで話が進んでから検討すべき。

### 3-3. 推奨

**ハッカソン提出（8/30まで）は道1（事前計算・静的サイト）で十分。**
- 案A・案Bとも「1ファイルの静的HTML」として完結させられる。
- サーバー費用ゼロ、運用の手間ゼロ、審査員がダウンロードしてブラウザで開くだけで動く。
- ライブ方式（道2）は、事業化を具体的に検討する段階（顧客が実際に使うプロダクトに
  なった時点）で再検討すればよい。

---

## 4. タスク

- [x] 頑健性チェックの実施 — **2026-08-05完了**（本ノート§1）
- [x] temperature=0をデフォルト化・日本語指定の追加 — **2026-08-05完了**
      （[[projects/ABM事業化/scripts/06_agent_layer_prototype.py]]）
- [ ] [P0|S] **選択肢A（LLMを説明役に限定する設計）に切り替える** — Why: §1で見つかった
      「穏やかな入力に鈍感」問題を学習不要で回避できる、最も現実的な対応 | 期限: 08/08
- [ ] [P1|S] 選択肢B（few-shot・chain-of-thought）を試し、Aとの効果を比較 — Why: 学習なしで
      さらに改善できる余地があるか確認 | 期限: 08/10
- [ ] [P2|XS] 事前計算JSONを04ダッシュボードに統合する（道1の実装） — Why: 案Bを
      Web公開可能な形にする | 期限: 08/12
- [ ] [P3|M] 本格的なファインチューニング（選択肢C）を別研究として検討するか
      — [[projects/流域スケール自然資本評価/00-研究構想]] との統合を検討 | 期限: ハッカソン後

## 関連
- [[projects/ABM事業化/06-案B設計と限界]]
- [[projects/ABM事業化/scripts/06_agent_layer_prototype.py]]
- [[projects/ABM事業化/scripts/07_robustness_check.py]]
- [[projects/ABM事業化/04-流出係数プロトタイプ]]
- [[context/about-me]]（Web公開の方針）
- [[decisions/log]] D-014 / D-015
