---
type: project-detail
name: リアルタイム自動レコメンド機能
parent: [[projects/ABM事業化/10-Ollama不要化とWebLLM検証・議論ツール化]]
status: 実装・実ブラウザで動作確認済み（バグ修正1件あり）
tags: [LLMエージェント, リアルタイム, レコメンド, UI, 検証]
created: 2026-08-05
updated: 2026-08-05
---

# リアルタイム自動レコメンド機能

> 2026-08-05、Shonadeshiko への確認質問「レコメンド機能というかタイで対応をしてほしい、
> というのはAIが能動的に提案する／リアルタイムで応答する、どちらの意図か」に対し、
> **「リアルタイムで応答する」**との回答を得た。これに基づき実装した機能を記録する。

---

## 0. 結論

**地図上のメッシュをクリックした瞬間、質問を打たなくてもAIが自動で短い推奨コメントを
生成する機能を実装し、実際のブラウザで動作確認した。** 実装過程で1件のバグを発見・
修正しており、その経緯も含めて正直に記録する。

---

## 1. 何を実装したか

これまでのチャットUI（[[projects/ABM事業化/09-オンデマンドチャットUIとDify統合設計]]）は、
ユーザーが自分でテキストを入力し「送信」を押す必要があった。
「リアルタイムで応答する」という要望を受け、**メッシュをクリックした時点で
自動的にAIが短い推奨コメントを生成し、チャットログに追加する**機能を追加した。

```
【従来】 メッシュをクリック → ユーザーが質問を入力 → 送信 → AIが応答
【今回】 メッシュをクリック → （何もしなくても）AIが自動で推奨コメントを生成・表示
        （その後、通常のチャット欄で追加の質問も可能）
```

- チェックボックスで有効/無効を切り替え可能（既定はON）。
- 同じメッシュ・同じシナリオ設定への再クリックはキャッシュから即答（再計算しない）。
- 連続して別のメッシュをクリックした場合、古いリクエストの結果は破棄する
  （世代カウンタ方式）。
- 既存の「メッシュID捏造検出」ガードレール（[[projects/ABM事業化/10-Ollama不要化とWebLLM検証・議論ツール化]] §3）
  は自動レコメンドにも適用済み。

---

## 2. 🔴 実装中に発見・修正したバグ

初回実装をChrome DevTools Protocol（実ブラウザ、リモートデバッグ）経由で検証したところ、
**クリックしても推奨コメントが一切表示されない**という不具合が発生した。

原因を`Runtime.evaluate`で直接関数を呼び出して調査したところ:

```
CALL_ERROR: currentSceneKey is not defined
```

**過去のバージョン（案B単発Q&A、[[projects/ABM事業化/08-地図データ直結型エージェントUI]]）で
定義していた`currentSceneKey()`関数を、チャットUIへの書き換え時（D-018）に削除して
いたが、新しい自動レコメンド機能のコードがこの関数を（削除されたことに気づかず）
参照し続けていた**、という単純な取り消し漏れだった。関数を再定義して解消した。

→ **これは「実際に実ブラウザで動かして検証する」ことの価値を裏付ける実例**。
コードの構文チェック（`node --check`相当）だけでは発見できず、実際に関数を
呼び出して初めて分かった不具合だった。

---

## 3. 検証結果（実ブラウザ、2026-08-05）

Chrome DevTools Protocol経由で、実際の（ヘッドレスでない）ブラウザに対して
地図上のメッシュをクリックするイベントを送信し、その後の挙動を監視した。

```
t=0秒:    クリックイベント発火
t=0〜15秒: ステータス表示「🔴 リアルタイム推奨コメントを生成中…」
t=約20秒:  チャットログに自動で推奨コメントが表示される
```

実際に得られた推奨コメント（抜粋）:

> この状況では以下の点が懸念されています：
> 1. **流出係数が高い**：流出係数 f = 0.899（2022年実測値との差: 0.007）は非常に高いです。
>    これは市街地化が進行していることを示唆しています。
> （中略）
> 監視を強化すべきエリア: 市街地の比率が高い地域や、流出係数が0.899以上の地域に
> 重点を置き、特に市街地化が進行している場所での流出係数の変動を定期的に監視する。

選択したメッシュの実際の値（f=0.899、市街地99.8%等）を正しく参照しており、
**この回答では実在しないメッシュIDの捏造も発生しなかった**（ただし過去の検証
（[[projects/ABM事業化/10-Ollama不要化とWebLLM検証・議論ツール化]] §3）で示した通り、
発生しないことを保証するものではなく、ガードレールは有効なままにしている）。

---

## 4. 所要時間についての正直な注意

自動レコメンドは1回あたり約15〜20秒かかる（Ollama, qwen2.5:1.5b、CPU推論）。
**「リアルタイム」ではあるが「即時（1秒未満）」ではない。** 複数のメッシュを
連続でクリックした場合、古いリクエストは破棄されるため表示は最新のものだけになるが、
呼び出し自体は毎回発生するため、短時間に何度もクリックすると裏でOllamaへの
リクエストが積み重なる可能性がある（世代カウンタで表示は正しく制御されるが、
リクエスト自体のキャンセルは行っていない）。

- [ ] [P2|XS] 連続クリック時に、表示されなかった古いリクエストの結果を
      実際にキャンセル（AbortController等）するか検討 — Why: 無駄な計算を減らすため
      | 期限: 未定

---

## 5. 🆕 チャットの見た目改善（2026-08-05追加）

Shonadeshikoより「チャットのところにコメントが多すぎるので見た目をよくしてください」
との依頼を受け、以下を実装した。

### 5-1. 問題

AIの応答には`**太字**`・`### 見出し`・箇条書き（`- `や`1. `）といったMarkdown記法が
自然に含まれるが、これまでは`textContent`でそのまま表示していたため、
**記号がそのまま文字として見えて読みにくかった**（例: `**流出係数**`という記号が
そのまま表示される）。

### 5-2. 実装した対策

1. **軽量Markdownレンダラー**を自作し導入（外部ライブラリ不使用）。
   まずHTMLエスケープしてから、こちらで許可した範囲のタグ（`strong`/`ul`/`ol`/`li`/`p`）
   だけを組み立てる設計にし、任意のHTML注入を防いだ。
2. **チャットバブルのデザイン刷新**: ユーザーの発言は右寄せ・青背景、AIの発言は
   左寄せ・白背景と、一般的なチャットアプリに近い見た目に変更。
3. **長い応答の折りたたみ**: 220文字を超える応答は要約表示し、
   「続きを見る（全文表示）」ボタンで全文展開できるようにした
   （チャット欄が長文で埋め尽くされるのを防ぐ）。

### 5-3. 🔴 実装中に発見・修正したバグ（番号付けの不具合）

初回実装を実ブラウザで確認したところ、**箇条書きの番号が「1. 1. 1.」と
毎回1に戻ってしまう**不具合を発見した。原因は、AIの応答が箇条書きの各項目の間に
**空行を挟む**ことが多く、レンダラーが空行のたびにリストを閉じてしまい、
次の項目で新しい`<ol>`（＝1から再スタート）を開始していたため。

**対策**: 空行が来た時、次の非空行を先読みして「同じ種類のリストが続くなら
空行を無視してリストを継続する」処理に変更した。修正後、実際に
`<ol><li>1</li><li>2</li><li>3</li></ol>`という1つの連続したリストとして
正しく生成されることを、実ブラウザのDOM構造を直接取得して確認した。

---

## 6. 関連

- [[projects/ABM事業化/04-流出係数プロトタイプ]]（本機能を実装したダッシュボード本体）
- [[projects/ABM事業化/10-Ollama不要化とWebLLM検証・議論ツール化]]（捏造検出ガードレール）
- [[projects/ABM事業化/09-オンデマンドチャットUIとDify統合設計]]（チャットUIの前身）
- [[decisions/log]] D-020 / D-021
